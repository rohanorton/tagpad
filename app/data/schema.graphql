schema {
  query: Query
}

type Item {
  title: String
  content: String
  id: String
}

type ItemsList {
  id: String
  items: [Item]
}

type Query {
  itemsList(id: String, title: String): ItemsList
}
